{
  "name": "server",
  "version": "1.0.0",
  "description": "Server-side (expressjs) for ToDoList App",
  "main": "server.js",
  "scripts": {
    "babel-node": "babel-node --presets=es2015",
    "start": "nodemon --exec npm run babel-node -- ./src/server.js",
    "build": "babel src -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Thet Min Ko",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.2"
  }
}
